import { Meta, Title, IconGallery, IconItem } from '@storybook/blocks';

import  Icon  from './Icon';
import * as icons from "./SmartSavingsIcon/index.mjs"

<Meta title="Iconography" />

# Iconography

<IconGallery>
    {Object.values(icons).map((icon) => {
        return  (
            <IconItem key={icon.name} name={icon.name}>
                <Icon name={icon.name} />
            </IconItem>
        )
    })}
</IconGallery>


## Color
### Success
<IconGallery>
    <IconItem key={'success'} name={Object.values(icons)[0].name}>
        <Icon name={Object.values(icons)[0].name}  color='success'/>
    </IconItem>
</IconGallery>
### Error

<IconGallery>
    <IconItem key={'error'} name={Object.values(icons)[0].name}>
        <Icon name={Object.values(icons)[0].name}  color='error'/>
    </IconItem>
</IconGallery>